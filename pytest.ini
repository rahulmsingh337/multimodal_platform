[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --timeout=30
markers =
    unit: Unit tests (no network required)
    e2e: End-to-end tests (requires live deployment)
    slow: Tests that take > 5 seconds
